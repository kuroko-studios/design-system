{
  "name": "@kuroko-studios/design",
  "version": "0.1.0",
  "description": "Kuroko Design System — machine-readable design tokens + components (dark brand v0).",
  "type": "module",
  "private": true,
  "files": [
    "dist",
    "components"
  ],
  "exports": {
    "./tokens.css": "./dist/kuroko-tokens.css",
    "./theme.css": "./dist/kuroko-theme.css",
    "./tokens.json": "./dist/tokens.json",
    "./components.css": "./components/components.css",
    "./components": "./components/index.ts"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  },
  "devDependencies": {
    "@types/react": "^19",
    "react": "^19",
    "style-dictionary": "^4.3.0",
    "typescript": "^5"
  }
}
